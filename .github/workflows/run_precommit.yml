---
name: prek

on:
    pull_request:
    push:
        branches: [master]

jobs:
    prek:
        runs-on: ubuntu-latest
        steps:
        -   uses: actions/checkout@v7
        -   uses: j178/prek-action@v1
